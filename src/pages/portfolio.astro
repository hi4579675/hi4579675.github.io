---
import Header from '../components/Header.astro';
---
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>portfolio — hanna.log</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&family=Noto+Sans+KR:wght@400;500&display=swap" rel="stylesheet" />
  <script is:inline>(function(){var t=localStorage.getItem('theme');if(t){document.documentElement.setAttribute('data-theme',t)}else{document.documentElement.setAttribute('data-theme','dark')}})()</script>
  <style is:global>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'JetBrains Mono', 'Noto Sans KR', sans-serif; background: #0d0f1a; color: #c8d3f5; min-height: 100vh; }


    main { max-width: 860px; margin: 0 auto; padding: 0 2.5rem 5rem; }

    .page-header { padding: 3.5rem 0 2rem; border-bottom: 1px solid #1e2340; }
    .page-title { font-size: 28px; font-weight: 500; color: #c8d3f5; }
    .page-title em { color: #82aaff; font-style: normal; }
    .page-desc { margin-top: .6rem; font-size: 13px; color: #636d94; font-family: 'Noto Sans KR', sans-serif; line-height: 1.8; }

    /* TABS */
    .tab-nav { display: flex; border-bottom: 1px solid #1e2340; margin: 1.8rem 0 1.4rem; }
    .tab-btn {
      font-family: 'JetBrains Mono', monospace; font-size: 11px; color: #444b6a;
      background: none; border: none; padding: 8px 16px; cursor: pointer;
      letter-spacing: .05em; border-bottom: 2px solid transparent; margin-bottom: -1px;
      transition: color .15s, border-color .15s;
    }
    .tab-btn:hover { color: #c8d3f5; }
    .tab-btn.active { color: #82aaff; border-bottom-color: #82aaff; }
    .tab-panel { display: none; }
    .tab-panel.active { display: block; }

    .project-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }

    .project-card {
      background: #0f1120; border: 1px solid #1e2340; border-radius: 10px; overflow: hidden;
      transition: border-color .2s, transform .15s; display: flex; flex-direction: column;
      cursor: pointer;
    }
    .project-card:hover { border-color: #2d3561; transform: translateY(-3px); }

    /* 썸네일 */
    .card-thumb {
      position: relative; overflow: hidden;
      aspect-ratio: 16/9; flex-shrink: 0;
    }
    .card-thumb img {
      width: 100%; height: 100%; object-fit: cover;
      transition: transform .4s ease;
      display: block;
    }
    .project-card:hover .card-thumb img { transform: scale(1.07); }

    /* 썸네일 위 GitHub 아이콘 */
    .card-gh {
      position: absolute; top: 10px; right: 10px;
      width: 30px; height: 30px; border-radius: 50%;
      background: rgba(9,11,20,.75); backdrop-filter: blur(4px);
      border: 1px solid rgba(255,255,255,.12);
      display: flex; align-items: center; justify-content: center;
      color: #c8d3f5; text-decoration: none;
      opacity: 0; transform: translateY(-4px);
      transition: opacity .2s, transform .2s, background .15s;
      z-index: 2;
    }
    .card-gh svg { width: 14px; height: 14px; fill: currentColor; }
    .project-card:hover .card-gh { opacity: 1; transform: translateY(0); }
    .card-gh:hover { background: rgba(130,170,255,.3); color: #82aaff; }

    /* 카드 본문 */
    .card-body { padding: 1.2rem 1.3rem 1.4rem; display: flex; flex-direction: column; gap: 10px; flex: 1; }
    .card-title { font-size: 15px; color: #c8d3f5; font-weight: 500; line-height: 1.35; font-family: 'JetBrains Mono', 'Noto Sans KR', sans-serif; }
    .card-desc { font-size: 13px; color: #636d94; font-family: 'Noto Sans KR', sans-serif; line-height: 1.75; flex: 1; }

    .card-tags { display: flex; gap: 5px; flex-wrap: wrap; }
    .tag { font-size: 11px; padding: 4px 12px; border-radius: 20px; letter-spacing: .03em; }

    .card-footer { display: flex; align-items: center; justify-content: space-between; padding-top: 10px; margin-top: 2px; }
    .card-detail-link {
      display: inline-flex; align-items: center; gap: 4px;
      font-size: 12px; color: #444b6a; text-decoration: none;
      font-family: 'Noto Sans KR', sans-serif;
      transition: color .15s;
    }
    .card-detail-link:hover { color: #82aaff; }
    .card-gh-link { display: inline-flex; align-items: center; gap: 5px; font-size: 11px; color: #2e3455; text-decoration: none; transition: color .15s; }
    .card-gh-link:hover { color: #636d94; }
    .card-gh-link svg { width: 13px; height: 13px; fill: currentColor; flex-shrink: 0; }
    .tb { background: rgba(130,170,255,.1);  color: #82aaff; border: 1px solid rgba(130,170,255,.2); }
    .tv { background: rgba(192,153,255,.1);  color: #c099ff; border: 1px solid rgba(192,153,255,.2); }
    .tc { background: rgba(134,225,252,.09); color: #86e1fc; border: 1px solid rgba(134,225,252,.18); }
    .tg { background: rgba(195,232,141,.09); color: #c3e88d; border: 1px solid rgba(195,232,141,.18); }
    .to { background: rgba(255,199,119,.09); color: #ffc777; border: 1px solid rgba(255,199,119,.18); }
    .tp { background: rgba(255,117,127,.09); color: #ff757f; border: 1px solid rgba(255,117,127,.18); }

    /* MODAL */
    .modal-overlay {
      display: none; position: fixed; inset: 0;
      background: rgba(9,11,20,.88); backdrop-filter: blur(6px);
      z-index: 300; align-items: center; justify-content: center; padding: 1rem;
    }
    .modal-overlay.open { display: flex; }

    .modal {
      background: #0f1120; border: 1px solid #2d3561; border-radius: 14px;
      width: 100%; max-width: 800px; height: 90vh; max-height: 900px; overflow-y: auto;
      position: relative; transform: translateY(20px); transition: transform .25s;
    }
    .modal-overlay.open .modal { transform: translateY(0); }

    .modal-close {
      position: sticky; top: 1rem; float: right; margin: 1rem 1rem 0 0;
      background: #0d0f1a; border: 1px solid #2d3561; color: #636d94; cursor: pointer;
      width: 30px; height: 30px; border-radius: 50%; font-size: 13px;
      display: flex; align-items: center; justify-content: center;
      transition: border-color .15s, color .15s; z-index: 10;
    }
    .modal-close:hover { border-color: #82aaff; color: #82aaff; }

    .modal-header {
      display: flex; gap: 1.25rem; padding: 1.5rem 1.5rem 1.25rem;
      border-bottom: 1px solid #1e2340; align-items: flex-start;
    }
    .modal-img { width: 120px; height: 80px; object-fit: cover; border-radius: 8px; flex-shrink: 0; }
    .modal-title-wrap { display: flex; flex-direction: column; gap: .35rem; min-width: 0; }
    .modal-title { font-size: 18px; font-weight: 700; color: #c8d3f5; font-family: 'JetBrains Mono', 'Noto Sans KR', sans-serif; line-height: 1.3; }
    .modal-subtitle { font-family: 'JetBrains Mono', 'Noto Sans KR', sans-serif; font-size: 11px; color: #444b6a; letter-spacing: .08em; }
    .modal-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: .2rem; }
    .modal-github-btn {
      display: inline-flex; align-items: center; gap: .4rem;
      font-size: 12px; font-weight: 600; color: #82aaff;
      text-decoration: none; margin-top: .4rem; transition: opacity .15s;
    }
    .modal-github-btn:hover { opacity: .75; }

    .modal-body { padding: 1.5rem; display: flex; flex-direction: column; gap: 1.5rem; }

    .modal-section-title {
      font-size: 10px; font-weight: 700; letter-spacing: .1em; text-transform: uppercase;
      color: #82aaff; margin-bottom: .6rem; padding-bottom: .4rem;
      border-bottom: 1px solid #1e2340; font-family: 'JetBrains Mono', 'Noto Sans KR', sans-serif;
    }
    .modal-text { font-size: 13.5px; color: #a9b1d6; line-height: 1.85; word-break: keep-all; font-family: 'Noto Sans KR', sans-serif; white-space: pre-wrap; }
    .modal-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .45rem; }
    .modal-list li {
      font-size: 13.5px; color: #a9b1d6; padding-left: 1rem;
      position: relative; line-height: 1.6; word-break: keep-all;
      font-family: 'Noto Sans KR', sans-serif;
    }
    .modal-list li::before { content: '·'; position: absolute; left: 0; color: #82aaff; }
    .modal-list li.role-header {
      font-family: 'JetBrains Mono', monospace;
      font-size: 10.5px; font-weight: 700; letter-spacing: .12em; text-transform: uppercase;
      color: #82aaff; padding-left: 0; margin-top: .9rem;
    }
    .modal-list li.role-header:first-child { margin-top: 0; }
    .modal-list li.role-header::before { content: ''; }

    @media (max-width: 860px) {
      .modal { max-width: 100%; height: 95vh; max-height: 95vh; }
    }
    @media (max-width: 640px) {
      .project-grid { grid-template-columns: 1fr; }
      .modal { height: 100vh; max-height: 100vh; border-radius: 0; }
      .modal-header { flex-direction: column; }
      .modal-img { width: 100%; height: 140px; }
      .modal-overlay { padding: 0; align-items: flex-end; }
    }

    footer { border-top: 1px solid #1e2340; padding: 1.5rem 2.5rem; text-align: center; font-size: 11px; color: #2e3455; letter-spacing: .08em; }
  </style>
</head>
<body>

<Header />

<main>
  <div class="page-header">
    <h1 class="page-title">port<em>folio</em></h1>
    <p class="page-desc">직접 설계하고 구현한 프로젝트들입니다.</p>
  </div>

  <div class="tab-nav">
    <button class="tab-btn active" data-tab="java">Java · Spring Boot</button>
    <button class="tab-btn" data-tab="fastapi">FastAPI · LangChain</button>
  </div>

  <!-- Java / Spring Boot -->
  <div class="tab-panel active" id="tab-java">
    <div class="project-grid">

    </div>
  </div>

  <!-- FastAPI / LangChain -->
  <div class="tab-panel" id="tab-fastapi">
    <div class="project-grid">

      <div class="project-card" data-modal="jikimi">
        <div class="card-thumb">
          <img src="https://images.unsplash.com/photo-1514888286974-6c03e2ca1dba?w=800&q=80" alt="계약지킴이" loading="lazy" />
          <a href="https://github.com/hi4579675/jikimi" target="_blank" class="card-gh" onclick="event.stopPropagation()">
            <svg viewBox="0 0 24 24"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.438 9.8 8.205 11.387.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.756-1.333-1.756-1.09-.745.083-.73.083-.73 1.205.085 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 21.795 24 17.295 24 12c0-6.63-5.37-12-12-12"/></svg>
          </a>
        </div>
        <div class="card-body">
          <div class="card-title">계약지킴이 (Jikimi)</div>
          <div class="card-desc">임차인을 위한 임대차 계약 AI 분석 서비스. 계약서(PDF/이미지)를 업로드하면 OCR → 법령 대조 → 독소 조항 리포트를 즉시 생성합니다.</div>
          <div class="card-tags">
            <span class="tag tv">FastAPI</span><span class="tag tb">Spring Boot</span><span class="tag tc">RAG</span><span class="tag tv">LLM</span><span class="tag to">OCR</span>
          </div>
          <div class="card-footer">
            <span class="card-detail-link">자세히 보기 →</span>
            
          </div>
        </div>
      </div>

      <div class="project-card" data-modal="nolleo-onna">
        <div class="card-thumb">
          <img src="/nolleo-onna-demo.png" alt="놀러온나" loading="lazy" />
          <a href="https://github.com/nolleo-onna/nolleo-onna-pipeline" target="_blank" class="card-gh" onclick="event.stopPropagation()">
            <svg viewBox="0 0 24 24"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.438 9.8 8.205 11.387.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.756-1.333-1.756-1.09-.745.083-.73.083-.73 1.205.085 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 21.795 24 17.295 24 12c0-6.63-5.37-12-12-12"/></svg>
          </a>
        </div>
        <div class="card-body">
          <div class="card-title">놀러온나 — TourAPI 데이터 파이프라인</div>
          <div class="card-desc">TourAPI 부산 업소 데이터와 행안부 착한가격업소를 상호명·주소·좌표 3중 매칭으로 정제·결합해 ‘정부 인증 가성비 업소’ 신규 데이터셋을 생성하는 파이프라인.</div>
          <div class="card-tags">
            <span class="tag tg">Python</span><span class="tag to">TourAPI</span><span class="tag tb">PostgreSQL</span><span class="tag tc">ETL</span>
          </div>
          <div class="card-footer">
            <span class="card-detail-link">자세히 보기 →</span>

          </div>
        </div>
      </div>

      <div class="project-card" data-modal="langchain">
        <div class="card-thumb">
          <img src="/langchain-demo.png" alt="LangChain 이슈서치" loading="lazy" />
          <a href="https://github.com/hi4579675/langchain-issue-search" target="_blank" class="card-gh" onclick="event.stopPropagation()">
            <svg viewBox="0 0 24 24"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.438 9.8 8.205 11.387.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.756-1.333-1.756-1.09-.745.083-.73.083-.73 1.205.085 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 21.795 24 17.295 24 12c0-6.63-5.37-12-12-12"/></svg>
          </a>
        </div>
        <div class="card-body">
          <div class="card-title">랭체인 119</div>
          <div class="card-desc">LangChain GitHub bug 이슈 3,600+개를 벡터화한 트러블슈팅 RAG 시스템. </div>
          <div class="card-tags">
            <span class="tag tg">LangChain</span><span class="tag tc">RAG</span><span class="tag tv">FastAPI</span><span class="tag tg">Python</span><span class="tag tb">PostgreSQL</span>
          </div>
          <div class="card-footer">
            <span class="card-detail-link">자세히 보기 →</span>
            
          </div>
        </div>
      </div>

    </div>
  </div>
</main>

<!-- MODAL -->
<div class="modal-overlay" id="modal-overlay">
  <div class="modal" id="modal-panel">
    <button class="modal-close" id="modal-close" aria-label="닫기">✕</button>
    <div class="modal-header">
      <img class="modal-img" id="modal-img" src="" alt="" />
      <div class="modal-title-wrap">
        <h2 class="modal-title" id="modal-title"></h2>
        <p class="modal-subtitle" id="modal-subtitle"></p>
        <div class="modal-tags" id="modal-tags"></div>
        <a class="modal-github-btn" id="modal-github" href="" target="_blank" rel="noopener">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.438 9.8 8.205 11.387.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.387-1.333-1.756-1.333-1.756-1.09-.745.083-.73.083-.73 1.205.085 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 21.795 24 17.295 24 12c0-6.63-5.37-12-12-12"/></svg>
          GitHub
        </a>
      </div>
    </div>
    <div class="modal-body">
      <section class="modal-section">
        <h3 class="modal-section-title">프로젝트 개요</h3>
        <p class="modal-text" id="modal-overview"></p>
      </section>
      <section class="modal-section">
        <h3 class="modal-section-title">내가 맡은 역할</h3>
        <ul class="modal-list" id="modal-role"></ul>
      </section>
      <section class="modal-section">
        <h3 class="modal-section-title">어려웠던 점</h3>
        <p class="modal-text" id="modal-challenge"></p>
      </section>
      <section class="modal-section">
        <h3 class="modal-section-title">해결 방법</h3>
        <p class="modal-text" id="modal-solution"></p>
      </section>
    </div>
  </div>
</div>

<footer>© 2026 hanna.log · Built with Astro & Starlight</footer>

<script>
  const modals = {
    jikimi: {
      img: 'https://images.unsplash.com/photo-1514888286974-6c03e2ca1dba?w=800&q=80',
      title: '계약지킴이 (Jikimi)',
      subtitle: 'JIKIMI',
      tags: `<span class="tag tv">FastAPI</span><span class="tag tb">Spring Boot</span><span class="tag tc">RAG</span><span class="tag tv">LLM</span><span class="tag to">OCR</span>`,
      overview: '임대차 계약 만료를 앞둔 어머니가 변호사 검토 비용 부담으로 그냥 서명하려 하셨습니다. 공인중개사는 계약 성사가 목적이라 임차인 편이 되기 어렵고, 법률 챗봇은 계약서를 직접 읽지 못합니다. 그 공백을 채우기 위해 계약서를 업로드하면 독소 조항을 즉시 탐지하는 AI 분석 서비스를 만들었습니다.',
      role: [
        'OCR 파이프라인부터 RAG 검색, LLM 분석, Spring Boot AI 통합 설계까지 전체 시스템을 단독 설계 및 구현했습니다.',
        'OCR 파이프라인: pdfplumber 기반 텍스트 추출, 조항 단위 파싱, 노이즈(페이지 번호·서명란·별지) 정제',
        'Hybrid RAG: BM25 + pgvector 벡터 검색을 RRF로 병합, 상가임대차보호법 36개 조문 임베딩 및 검색',
        'LLM 분석: GPT-4o 기반 독소 조항 판별, Hallucination 방지를 위한 법령 조항 교차 검증 프롬프트 설계',
        'Spring Boot AI 통합: AIServiceClient 인터페이스 추상화, Resilience4j Circuit Breaker + TimeLimiter로 AI 장애 격리, 202 Accepted + polling 비동기 패턴 구현',
        '인프라: Docker Compose 기반 PostgreSQL + pgvector 환경 구성, AWS 배포',
      ],
      challenge: '1. 법령 참조문이 조항으로 오탐: 본문 중간의 "제10조의4제1항" 같은 법령 인용문이 실제 조항 번호로 잘못 파싱되어 12개 조항이 21개로 뻥튀기됨.\n2. 페이지 번호가 조항 내용에 삽입: pdfplumber가 PDF 레이아웃을 선형 텍스트로 변환하는 과정에서 "- 1 / 3 -"가 제8조 본문 중간에 끼어들어 내용이 잘림.\n3. async 함수 내 동기 블로킹: async def search_legal_context 안에서 psycopg2(동기)와 Gemini 클라이언트(동기)를 직접 호출하여 이벤트 루프 전체가 블로킹됨.',
      solution: '1. 줄 시작(^) + 괄호 제목이 바로 뒤따르는 패턴만 실제 조항으로 인식하도록 정규식 변경. 본문 중간의 법령 참조문은 줄 시작에 위치하지 않으므로 자연스럽게 필터링됨.\n   전: pattern = r\'(제\\d+조(?:의\\d+)?)\'\n   후: pattern = r\'(?m)^[ \\t]*(제\\d+조(?:의\\d+)?)\\s*[（(]\'\n2. 페이지 번호 앞뒤 줄바꿈까지 함께 제거하는 정규식으로 해결. 단순 패턴 제거가 아니라 앞뒤 \\n*를 묶어서 내용 사이 빈 줄이 남지 않도록 처리.\n3. 동기 블로킹 로직을 _embed, _query_db로 분리하고 asyncio.to_thread()로 위임. asyncpg 교체도 검토했으나 PoC 단계에서 드라이버 교체는 오버엔지니어링이라 판단하고 to_thread로 블로킹만 해소.',
      github: 'https://github.com/hi4579675/jikimi',
    },
    'nolleo-onna': {
      img: '/nolleo-onna-demo.png',
      title: '외부 공공 API를 서비스 데이터로 바꾸는 부산 여행 파이프라인 (놀러온나)',
      subtitle: 'NOLLEO-ONNA',
      tags: `<span class="tag tg">Python</span><span class="tag to">TourAPI</span><span class="tag tb">PostgreSQL</span><span class="tag tc">ETL</span>`,
      overview: '한국관광공사 TourAPI 등 외부 공공 데이터를 부산 여행 서비스에 적재하기 위한 데이터 파이프라인. 외부 API 의존을 사용자 응답 경로에서 분리하기 위해 수집·정제·LLM 가공·임베딩 적재를 별도 Python 파이프라인으로 두고, 서비스(Spring Boot)는 PostgreSQL 마스터 데이터만 읽음.',
      role: [
        '데이터 파이프라인, 데이터베이스 모델링, 운영 안전장치 설계까지 데이터 레이어를 단독 작업.',
        '**구현 완료**',
        '공용 인프라: psycopg 3.x 비동기 풀, httpx + tenacity 재시도, KST/UTC 시간 유틸, SHA-256 해시, async with sync_log_run() 컨텍스트 매니저',
        'TourAPI 관광지 sync 파이프라인: 4 endpoint(detailCommon2/Intro2/Info2/Image2) asyncio.gather 병렬 수집, Pydantic 정규화, UPSERT/REPLACE 단일 트랜잭션 적재',
        '비용 통제 4단계: 변경분 필터 / 워터마크 break + bootstrap 게이팅 / 일일 예산 하드가드 / 타입별 예산 분할 + 타입별 cursor',
        'Alembic 마이그레이션 6개: 스팟 도메인 7테이블 + 성능 인덱스 + updated_at 트리거 + SYNC_LOGS (외부 FK는 master 테이블 의존성으로 별도 보관)',
        'Docker Compose 로컬 환경 + AWS RDS ap-northeast-2 (PostgreSQL 16 + PostGIS + pgvector + pg_trgm)',
        '**설계 완료, 구현은 후속 작업**',
        '30+ 테이블 ERD: 스팟 외 행사·공식 코스·착한가격·한끗·태그·날씨 등 12개 도메인',
        'ADR 0001 — LLM 매칭 전략: 룰 점수(전화 0.5 / 이름 Jaro-Winkler 0.3 / 주소 0.15 / 거리 0.05) 자동 처리 + 회색지대(0.65~0.85)만 GPT-4o-mini로 위임',
        'ADR 0003 — 비활성 처리 4중 안전 가드',
        '신뢰도 큐 패턴 (BUSINESS_HOURS_REVIEW_QUEUE) — LLM 영업시간 정규화 도입 시',
        '2트랙 테스트 (mock 단위 + testcontainers 통합)',
      ],
      challenge: '1. TourAPI 일일 quota 1,000콜 vs 적재량 28,000콜: 부산 7,000 스팟 × 4 endpoint = 28,000콜. 단순 부트스트랩이 ~28일 걸리는 구조. 한 번 quota 다 태우면 그날 잡 즉시 중단. 순차 처리 시 contentTypeId=12에만 quota 다 쓰면 14·39는 영원히 못 들어가는 starvation 발생.\n\n2. 워터마크 최적화가 부트스트랩을 막음: arrange=C(수정일 역순) + 페이지 전부 unchanged면 break 로직을 넣었는데, 14번째 cron 실행에서 records_fetched=0. page 1의 5건이 모두 어제 sync된 거라 break 떨어지고, page 2 이후는 한 번도 본 적 없는 데이터인데 평생 누락.\n\n3. REPLACE 패턴의 분리 트랜잭션 노출 가능성 + PK 충돌: 갤러리 통째 교체(SPOT_IMAGES)에서 DELETE/INSERT를 분리 트랜잭션으로 두면 그 사이 SELECT가 빈 결과를 받을 수 있음. 또 ERD가 SPOT_TAGS PK를 (content_id, tag_id)로 정의했는데 정책은 LLM 재추출 시 source=\'llm\'만 교체해야 함 — 같은 (content_id, tag_id)에 manual·llm 공존 시 PK 충돌.',
      solution: '1. 4단계 quota 방어 설계.\n   - Layer 1 (변경분 필터): list 응답의 modifiedtime을 DB의 source_modified_time과 비교해 같으면 detail 4콜 호출 자체 skip\n   - Layer 2 (워터마크 break): bootstrap_complete=True일 때만 활성화 (해결 방법 2)\n   - Layer 3 (예산 하드가드): 900콜 도달 시 즉시 종료 + cursor 저장으로 다음 실행 재개\n   - Layer 4 (타입별 예산 분할): {"12": 0.5, "14": 0.25, "39": 0.25} 비율 + 타입별 cursor 분리 저장 → 한 타입의 quota 고갈이 다른 타입을 굶기지 않음\n\n2. bootstrap_complete 플래그 게이팅. 워터마크 break는 부트스트랩이 진짜로 끝났을 때만 안전. 모든 type의 cursor가 마지막 페이지에 도달한 시점에만 bootstrap_complete=true로 전환하고, 그 전엔 unchanged 페이지를 만나도 다음 페이지로 진행.\n   전: if page_unchanged_count == len(items): break\n   후: if not bootstrap_mode and page_unchanged_count == len(items): break\n\n3. 단일 트랜잭션 강제 + PK에 source 컬럼 추가.\n   - SPOT_IMAGES: async with conn.transaction() 블록 안에서 DELETE + INSERT executemany를 묶음. COMMIT 시점에만 외부 노출.\n   - SPOT_TAGS: PK를 (content_id, tag_id, source) 3컬럼으로 확장 → 같은 (content_id, tag_id)에 manual·llm 공존 가능. DELETE WHERE source=\'llm\'만 돌려도 manual 행은 자연 보존.',
      github: 'https://github.com/nolleo-onna/nolleo-onna-pipeline',
    },
    langchain: {
      img: '/langchain-demo.png',
      title: '랭체인 119',
      subtitle: 'LANGCHAIN-ISSUE-INSIGHT',
      tags: `<span class="tag tg">LangChain</span><span class="tag tc">RAG</span><span class="tag tv">FastAPI</span><span class="tag tg">Python</span><span class="tag tb">PostgreSQL</span>`,
      overview: 'LangChain을 쓰다 보면 공식 문서에 없는 에러를 자주 만납니다. GPT-4에 물어보면 "버전이 달라서 모른다"는 답변만 돌아오고, Stack Overflow엔 아직 답이 없는 경우도 많습니다. 그래서 깃허브 이슈를 수집하여 트러블슈팅 RAG 시스템을 만들었습니다.',
      role: [
        '데이터 수집 파이프라인부터 RAG 파이프라인, API 서버, 평가 프레임워크까지 전체 시스템을 단독 설계 및 구현했습니다.',
        '데이터 수집: GitHub API rate limit 자동 제어 클라이언트, 이슈·댓글 수집 및 노이즈 정제',
        'RAG 파이프라인: 코드블록 분리 청킹, Gemini 배치 임베딩, pgvector 저장',
        '검색·리랭킹: 벡터 유사도 × 키워드 매칭 × solution 가중치 × 최신성 점수 결합',
        '평가 프레임워크: Hit@k, MRR, NDCG 지표 구현, 검색 구성별 ablation 실험 설계 및 실행',
        '인프라: Docker Compose 기반 PostgreSQL + pgvector 환경 구성',
      ],
      challenge: '1. BTree 인덱스 크기 초과: 코드블록이 긴 청크는 content 길이가 BTree 인덱스 한계(2,704 bytes)를 초과하여 upsert 실패.\n2. 수집 중 무한 대기: 특정 이슈에서 Gemini API 또는 GitHub API 응답이 돌아오지 않아 파이프라인 전체가 멈추는 현상 발생.\n3. solution 댓글 선별 기준 없음: GitHub 이슈 댓글 중 어떤 것이 실제 해결책인지 판단하는 레이블이 없음.\n4. 키워드 매칭 효과 미검증: Hybrid Reranking의 각 가중치를 직관으로 설정했으나 실제 효과를 수치로 확인하지 못한 상태.',
      solution: '1. content 전체 대신 MD5 해시를 인덱싱하고, UPSERT의 ON CONFLICT 절도 md5(content) 표현식으로 맞춤.\n2. 이슈 단위로 별도 Process를 생성하고 p.join(timeout=180)으로 OS 레벨 강제 종료. Thread와 달리 Process.kill()은 실제로 멈추기 때문에 스레드 기반 타임아웃의 한계를 우회.\n3. 댓글의 👍 reactions 수가 가장 높은 댓글을 solution으로 간주. 단, "thanks" / "+1" 등 노이즈 패턴과 30자 미만 댓글을 사전 필터링하여 정확도 보완.\n4. eval/compare.py로 구성별 ablation 실험 설계. 키워드 매칭이 MRR +36.8%로 가장 큰 기여를 했으며, 최신성 점수는 이 데이터셋에서 효과가 제한적임을 수치로 확인.',
      github: 'https://github.com/hi4579675/langchain-issue-search',
    },
  };

  const overlay = document.getElementById('modal-overlay');

  function openModal(key) {
    const m = modals[key];
    if (!m) return;
    document.getElementById('modal-img').src = m.img || '';
    document.getElementById('modal-img').alt = m.title;
    document.getElementById('modal-title').textContent = m.title;
    document.getElementById('modal-subtitle').textContent = m.subtitle || '';
    document.getElementById('modal-tags').innerHTML = m.tags;
    const ghBtn = document.getElementById('modal-github');
    ghBtn.href = m.github;
    document.getElementById('modal-overview').textContent = m.overview || '';
    const roleEl = document.getElementById('modal-role');
    roleEl.innerHTML = (m.role || []).map(r => {
      if (r.startsWith('**') && r.endsWith('**')) {
        return `<li class="role-header">${r.slice(2, -2)}</li>`;
      }
      return `<li>${r}</li>`;
    }).join('');
    document.getElementById('modal-challenge').textContent = m.challenge || '';
    document.getElementById('modal-solution').textContent = m.solution || '';
    overlay.classList.add('open');
    document.body.style.overflow = 'hidden';
  }

  function closeModal() {
    overlay.classList.remove('open');
    document.body.style.overflow = '';
  }

  // 탭 전환
  document.querySelectorAll('.tab-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
      document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
      btn.classList.add('active');
      document.getElementById('tab-' + btn.dataset.tab).classList.add('active');
    });
  });

  document.querySelectorAll('.project-card').forEach(card => {
    card.addEventListener('click', () => openModal(card.dataset.modal));
  });

  document.getElementById('modal-close').addEventListener('click', closeModal);
  overlay.addEventListener('click', e => { if (e.target === overlay) closeModal(); });
  document.addEventListener('keydown', e => { if (e.key === 'Escape') closeModal(); });
</script>

</body>
</html>
